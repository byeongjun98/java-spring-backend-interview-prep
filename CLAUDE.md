# CLAUDE.md — backend-prep

**백엔드 개발자** 채용 준비용 저장소. 진행 상황/체크리스트는 `README.md` 참고 (여기서 중복 기록 안 함).

## 이 프로젝트의 목적

경력 대비 실제 Java/Spring 구현력이 낮다는 자가진단 기반. 이론 학습(`docs/`)과 토이프로젝트(`project/`)를 병행하며 기초부터 다시 다진다. 목표는 "아는 척"이 아니라 설계 면접(1차 인터뷰, 50분)에서 왜 이렇게 설계했는지 직접 설명 가능한 수준.

## 다른 준비 저장소와 구분

- 같은 회사의 다른 포지션(Applied Algorithm Engineer, TypeScript, matching/scoring) 준비용 저장소가 별도로 있음 — 이 저장소엔 포함하지 않음. **섞지 말 것.**
- 이 저장소: 백엔드 개발자 포지션, Java/Spring, MSA, 대용량 트래픽/분산 처리 중심.
- saga/재고 락/결제 멱등성은 예전에 다른 프로젝트에서 다뤘지만 제대로 이해하고 넘어간 게 아니라서, **이 저장소에서 처음 배우는 것으로 취급하고 04·05·06번 문서에서 기초부터 다시 다짐** (이전 경험을 전제로 설명 생략하지 말 것).

## project/ 컨벤션 (설계 예정 — 실제 스캐폴딩 시 확정)

- 언어/빌드: Java 17+, Spring Boot, Gradle
- 테스트: JUnit5 + Mockito. 프레임워크 최소화, 표준 스택 우선.
- 학습용 저장소이므로 "왜 이렇게 짰는가"를 설명하는 주석을 적극적으로 남긴다 (일반 프로덕션 관례와 다름).
- 의도적 단순화는 `// ponytail: ...` 주석으로 한계와 업그레이드 조건 명시.

## docs/ 컨벤션

- 번호 순서로 학습 진행 (`00-...`). 새 문서는 다음 번호 사용.
- 공고 원문 문구를 최대한 그대로 인용해서 어떤 학습이 어떤 요구사항에 대응하는지 추적 가능하게 함.
- 구조: "이 요구사항이 왜 나왔나 → 핵심 개념 → 실습(project/ 대응 모듈) → 검색용 키워드".

## 세션 시작 시 참고할 것

- 지금까지 뭘 했는지 / 다음에 뭘 할지: `README.md`
